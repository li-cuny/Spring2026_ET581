## 📝 Practice Exercises Lab2

### Exercise 1
Write a program to find the length of a string entered by the user.

---

### Exercise 2
Compare two strings using `equals()` and display whether they are equal.

---

### Exercise 3
Check a username using `equalsIgnoreCase()`.

---

### Exercise 4
Convert a string to:
- lowercase
- uppercase

---

### Exercise 5
Remove extra spaces from a string using `trim()` and print the new length.

---

### Exercise 6
Print the first and last characters of the word `"Programming"`.

---

### Exercise 7
Extract `"happy"` from the string `"unhappy"` using `substring()`.

---

### Exercise 8
Extract `"computer"` from `"computer science"`.

---

### Exercise 9
Find the index of `"programming"` in `"Java programming is fun"`.

---

### Exercise 10
Find the second occurrence of `"one"` in  
`"one two one two one"`.

---

### Exercise 11
Use `lastIndexOf()` to find the last `"red"` in  
`"red blue red green red"`.

---

### Exercise 12
Compare two strings using `compareTo()` and print which comes first alphabetically.

---

### Exercise 13
Compare `"Hello"` and `"hello"` using `compareToIgnoreCase()`.

---

### Exercise 14 ⭐ Mini Challenge
1. Store `"   I hate text processing!   "`
2. Remove extra spaces
3. Replace `"hate"` with `"love"`
4. Print the final string

**Expected Output:**  
`I love text processing!`